name: SEO Agent — Daily Content Generation

on:
  schedule:
    # Every day at 08:00 UTC
    - cron: '0 8 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-post:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Anthropic SDK
        run: npm install @anthropic-ai/sdk

      - name: Check pause flag
        id: pause
        run: |
          if [ -f "scripts/paused.flag" ]; then
            echo "paused=true" >> $GITHUB_OUTPUT
            echo "Agent is paused — skipping."
          else
            echo "paused=false" >> $GITHUB_OUTPUT
          fi

      - name: Run SEO agent
        if: steps.pause.outputs.paused != 'true'
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: node scripts/seo-agent.mjs

      - name: Commit pending draft
        if: steps.pause.outputs.paused != 'true'
        run: |
          git config user.name "SEO Agent"
          git config user.email "seo-agent@ai-desk.tech"
          git add scripts/pending-post.json public/llms.txt
          if git diff --cached --quiet; then
            echo "No new draft to commit."
          else
            git commit -m "draft: pending post awaiting approval [skip ci]"
            git push
          fi
